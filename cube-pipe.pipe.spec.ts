import { CubePipePipe } from './cube-pipe.pipe';

describe('CubePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CubePipePipe();
    expect(pipe).toBeTruthy();
  });
});

import { CurrencyConvertorPipePipe } from './currency-convertor-pipe.pipe';

describe('CurrencyConvertorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyConvertorPipePipe();
    expect(pipe).toBeTruthy();
  });
});

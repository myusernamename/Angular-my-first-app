import { MemoryConvertPipePipe } from './memory-convert-pipe.pipe';

describe('MemoryConvertPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MemoryConvertPipePipe();
    expect(pipe).toBeTruthy();
  });
});

import { FourtimesPipePipe } from './fourtimes-pipe.pipe';

describe('FourtimesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FourtimesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
